# 听脑AI官网 - 完整动效实现方案

## 📋 目录

1. [高优先级动效](#高优先级动效)
2. [中优先级动效](#中优先级动效)
3. [微交互动效](#微交互动效)
4. [性能优化建议](#性能优化建议)
5. [实现步骤](#实现步骤)

---

## 高优先级动效

### 1. 标题文字逐字显示动画 (TextReveal)

**效果描述**：标题文字逐字淡入+上浮，营造打字机效果

**应用位置**：
- Hero区主标题："听脑AI，提高了10倍工作/学习效率"
- 各section标题

**代码实现**：

```typescript
// src/components/animations/TextReveal.tsx
import { motion } from "framer-motion";

interface TextRevealProps {
  text: string;
  className?: string;
  delay?: number;
  duration?: number;
}

export function TextReveal({ 
  text, 
  className = "", 
  delay = 0,
  duration = 0.05 
}: TextRevealProps) {
  const characters = text.split('');
  
  return (
    <motion.div 
      className={`inline-flex ${className}`}
      initial="hidden"
      whileInView="visible"
      viewport={{ once: true, margin: "-100px" }}
    >
      {characters.map((char, index) => (
        <motion.span
          key={`${char}-${index}`}
          variants={{
            hidden: { opacity: 0, y: 20 },
            visible: { opacity: 1, y: 0 }
          }}
          transition={{
            duration: 0.4,
            delay: delay + index * duration,
            ease: [0.25, 0.1, 0.25, 1]
          }}
        >
          {char === ' ' ? '\u00A0' : char}
        </motion.span>
      ))}
    </motion.div>
  );
}
```

**使用示例**：

```tsx
// 在首页中使用
<TextReveal 
  text="听脑AI，提高了" 
  className="font-['HarmonyOS_Sans_SC:Bold',sans-serif] text-[54px] text-[#191f33]"
/>
<TextReveal 
  text="10倍" 
  className="bg-clip-text bg-gradient-to-b text-[transparent] to-[#2ad1f8] from-[#0f67fe] text-[54px]"
  delay={0.6}
/>
<TextReveal 
  text="工作/学习效率" 
  className="font-['HarmonyOS_Sans_SC:Bold',sans-serif] text-[54px] text-[#191f33]"
  delay={0.8}
/>
```

---

### 2. CTA按钮发光/呼吸效果 (GlowButton)

**效果描述**：按钮周期性发光，边框光晕流动，吸引点击

**应用位置**：
- "免费使用"按钮
- "立即使用"按钮

**代码实现**：

```typescript
// src/components/animations/GlowButton.tsx (增强版)
import { motion } from "framer-motion";
import { PropsWithChildren } from "react";

interface GlowButtonProps {
  className?: string;
  onClick?: () => void;
  glowColor?: string;
}

export function GlowButton({ 
  children, 
  className = "",
  onClick,
  glowColor = "rgba(16, 139, 254, 0.6)"
}: PropsWithChildren<GlowButtonProps>) {
  return (
    <motion.button
      className={`relative overflow-hidden ${className}`}
      onClick={onClick}
      whileHover={{ scale: 1.05 }}
      whileTap={{ scale: 0.95 }}
      transition={{ type: "spring", stiffness: 400, damping: 17 }}
    >
      {/* 呼吸光晕 */}
      <motion.div
        className="absolute inset-0 rounded-[inherit]"
        style={{
          boxShadow: `0 0 20px ${glowColor}, 0 0 40px ${glowColor}`,
        }}
        animate={{
          opacity: [0.5, 1, 0.5],
        }}
        transition={{
          duration: 2,
          repeat: Infinity,
          ease: "easeInOut"
        }}
      />
      
      {/* 流动光效 */}
      <motion.div
        className="absolute inset-0 rounded-[inherit]"
        style={{
          background: `linear-gradient(90deg, transparent, ${glowColor}, transparent)`,
        }}
        animate={{
          x: ['-100%', '200%'],
        }}
        transition={{
          duration: 3,
          repeat: Infinity,
          ease: "linear"
        }}
      />
      
      {/* 按钮内容 */}
      <div className="relative z-10">
        {children}
      </div>
    </motion.button>
  );
}
```

**使用示例**：

```tsx
// 替换现有的 BackgroundImage2 组件
<GlowButton 
  className="bg-[#191f33] h-[40px] rounded-[10.37px]"
  glowColor="rgba(16, 139, 254, 0.6)"
>
  <div className="px-[20px] py-[5.185px]">
    <p className="text-[#f8f8f8] text-[16px]">免费使用</p>
  </div>
</GlowButton>
```

---

### 3. 场景切换标签动画 (TabIndicator)

**效果描述**：标签切换时，指示器平滑滑动，内容淡入淡出

**应用位置**：
- 场景切换区域（会议记录/学习辅助/销售通话/内容创作）

**代码实现**：

```typescript
// src/components/animations/TabIndicator.tsx
import { motion, AnimatePresence } from "framer-motion";
import { useState } from "react";

interface Tab {
  label: string;
  content: React.ReactNode;
}

interface TabIndicatorProps {
  tabs: Tab[];
  defaultIndex?: number;
  className?: string;
}

export function TabIndicator({ 
  tabs, 
  defaultIndex = 0,
  className = "" 
}: TabIndicatorProps) {
  const [activeIndex, setActiveIndex] = useState(defaultIndex);

  return (
    <div className={className}>
      {/* 标签栏 */}
      <div className="flex gap-[16px] relative">
        {tabs.map((tab, index) => (
          <button
            key={index}
            onClick={() => setActiveIndex(index)}
            className={`relative px-[24px] py-[12px] text-[16px] transition-colors duration-300 ${
              activeIndex === index 
                ? "text-[#191f33] font-medium" 
                : "text-[#75859d]"
            }`}
          >
            {tab.label}
            
            {/* 激活指示器 */}
            {activeIndex === index && (
              <motion.div
                layoutId="activeTab"
                className="absolute bottom-0 left-0 right-0 h-[3px] bg-gradient-to-r from-[#0f67fe] to-[#2ad1f8] rounded-full"
                transition={{ 
                  type: "spring", 
                  stiffness: 500, 
                  damping: 30 
                }}
              />
            )}
          </button>
        ))}
      </div>

      {/* 内容区 */}
      <div className="mt-[32px]">
        <AnimatePresence mode="wait">
          <motion.div
            key={activeIndex}
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            exit={{ opacity: 0, y: -20 }}
            transition={{ duration: 0.3 }}
          >
            {tabs[activeIndex].content}
          </motion.div>
        </AnimatePresence>
      </div>
    </div>
  );
}
```

**使用示例**：

```tsx
// 在场景切换区域使用
const scenarioTabs = [
  {
    label: "会议记录",
    content: <ScenarioContent 
      title="会议记录"
      description="实时记录会议内容并自动转写，快速生成会议纪要和待办事项"
      features={["实时转写", "自动纪要", "团队协作", "知识管理"]}
      image="/images/meeting.png"
    />
  },
  // ... 其他场景
];

<TabIndicator tabs={scenarioTabs} />
```

---

### 4. 特性图标弹出动画 (IconPop)

**效果描述**：图标依次弹出，带轻微旋转和缩放

**应用位置**：
- 功能特性列表（准确率98%、自动区分发言人等）
- 场景特性图标

**代码实现**：

```typescript
// src/components/animations/IconPop.tsx
import { motion } from "framer-motion";
import { PropsWithChildren } from "react";

interface IconPopProps {
  delay?: number;
  className?: string;
}

export function IconPop({ 
  children, 
  delay = 0,
  className = "" 
}: PropsWithChildren<IconPopProps>) {
  return (
    <motion.div
      className={className}
      initial={{ scale: 0, rotate: -180, opacity: 0 }}
      whileInView={{ scale: 1, rotate: 0, opacity: 1 }}
      viewport={{ once: true, margin: "-50px" }}
      transition={{
        type: "spring",
        stiffness: 260,
        damping: 20,
        delay
      }}
      whileHover={{ 
        scale: 1.1, 
        rotate: 5,
        transition: { duration: 0.2 }
      }}
    >
      {children}
    </motion.div>
  );
}
```

**批量使用组件**：

```typescript
// src/components/animations/IconPopList.tsx
import { IconPop } from "./IconPop";

interface IconPopListProps {
  children: React.ReactNode[];
  staggerDelay?: number;
  className?: string;
}

export function IconPopList({ 
  children, 
  staggerDelay = 0.1,
  className = "" 
}: IconPopListProps) {
  return (
    <div className={className}>
      {children.map((child, index) => (
        <IconPop key={index} delay={index * staggerDelay}>
          {child}
        </IconPop>
      ))}
    </div>
  );
}
```

**使用示例**：

```tsx
// 包裹特性图标
<IconPop delay={0}>
  <div className="bg-[rgba(16,139,254,0.2)] rounded-full size-[28px]">
    {/* 图标内容 */}
  </div>
</IconPop>

// 或批量使用
<IconPopList 
  staggerDelay={0.15}
  className="flex gap-[18px]"
>
  {features.map(feature => (
    <div className="bg-[rgba(16,139,254,0.2)] rounded-full size-[28px]">
      {feature.icon}
    </div>
  ))}
</IconPopList>
```

---

### 5. 数字滚动计数 (CountUp)

**效果描述**：数字从0递增到目标值，带缓动效果

**应用位置**：
- "10倍"效率提升
- "98%"准确率
- 其他数据指标

**代码实现**：

```typescript
// src/components/animations/CountUp.tsx
import { motion, useSpring, useTransform } from "framer-motion";
import { useEffect, useRef } from "react";
import { useInView } from "framer-motion";

interface CountUpProps {
  end: number;
  duration?: number;
  decimals?: number;
  suffix?: string;
  prefix?: string;
  className?: string;
}

export function CountUp({ 
  end, 
  duration = 2,
  decimals = 0,
  suffix = "",
  prefix = "",
  className = "" 
}: CountUpProps) {
  const ref = useRef<HTMLSpanElement>(null);
  const isInView = useInView(ref, { once: true, margin: "-100px" });
  
  const spring = useSpring(0, {
    stiffness: 100,
    damping: 30,
    duration: duration * 1000
  });
  
  const display = useTransform(spring, (value) => 
    prefix + value.toFixed(decimals) + suffix
  );

  useEffect(() => {
    if (isInView) {
      spring.set(end);
    }
  }, [isInView, end, spring]);

  return (
    <motion.span ref={ref} className={className}>
      {display}
    </motion.span>
  );
}
```

**使用示例**：

```tsx
// 替换静态数字
<div className="flex items-center gap-[12px]">
  <p className="text-[#191f33]">听脑AI，提高了</p>
  <CountUp 
    end={10} 
    suffix="倍"
    className="bg-clip-text bg-gradient-to-b text-[transparent] to-[#2ad1f8] from-[#0f67fe] text-[54px] font-bold"
  />
  <p className="text-[#191f33]">工作/学习效率</p>
</div>

// 准确率
<CountUp 
  end={98} 
  suffix="%"
  className="text-[#4e5765] font-bold"
/>
```

---

## 中优先级动效

### 6. 评价卡片星级动画 (StarRating)

**效果描述**：星星依次点亮，带闪烁效果

**代码实现**：

```typescript
// src/components/animations/StarRating.tsx
import { motion } from "framer-motion";

interface StarRatingProps {
  rating: number;
  maxRating?: number;
  size?: number;
  className?: string;
}

export function StarRating({ 
  rating, 
  maxRating = 5,
  size = 18,
  className = "" 
}: StarRatingProps) {
  return (
    <div className={`flex gap-[11px] ${className}`}>
      {Array.from({ length: maxRating }).map((_, index) => (
        <motion.span
          key={index}
          initial={{ scale: 0, opacity: 0 }}
          whileInView={{ scale: 1, opacity: 1 }}
          viewport={{ once: true }}
          transition={{
            delay: index * 0.1,
            type: "spring",
            stiffness: 500,
            damping: 15
          }}
          style={{ fontSize: size }}
        >
          {index < rating ? (
            <motion.span
              animate={{ 
                scale: [1, 1.2, 1],
              }}
              transition={{
                delay: index * 0.1 + 0.3,
                duration: 0.3
              }}
            >
              🌟
            </motion.span>
          ) : (
            "⭐"
          )}
        </motion.span>
      ))}
    </div>
  );
}
```

**使用示例**：

```tsx
// 替换静态星级
<StarRating rating={5} size={18} />
```

---

### 7. 背景渐变流动效果

**效果描述**：Hero区背景渐变色缓慢流动

**代码实现**：

```typescript
// src/components/animations/FlowingGradient.tsx
import { motion } from "framer-motion";

interface FlowingGradientProps {
  className?: string;
}

export function FlowingGradient({ className = "" }: FlowingGradientProps) {
  return (
    <motion.div
      className={`absolute inset-0 ${className}`}
      style={{
        background: "linear-gradient(120deg, #D9EAFF, #BBF1FF, #C9E3FF, #D9EAFF)",
        backgroundSize: "300% 300%",
      }}
      animate={{
        backgroundPosition: ["0% 50%", "100% 50%", "0% 50%"],
      }}
      transition={{
        duration: 15,
        repeat: Infinity,
        ease: "linear"
      }}
    />
  );
}
```

**使用示例**：

```tsx
// 在Hero区背景中使用
<div className="relative">
  <FlowingGradient className="opacity-60 blur-3xl" />
  {/* 其他内容 */}
</div>
```

---

### 8. 视差滚动效果 (ParallaxSection)

**效果描述**：元素随滚动以不同速度移动

**代码实现**：

```typescript
// src/components/animations/ParallaxSection.tsx
import { motion, useScroll, useTransform } from "framer-motion";
import { useRef, PropsWithChildren } from "react";

interface ParallaxSectionProps {
  speed?: number;
  className?: string;
}

export function ParallaxSection({ 
  children, 
  speed = 0.5,
  className = "" 
}: PropsWithChildren<ParallaxSectionProps>) {
  const ref = useRef<HTMLDivElement>(null);
  const { scrollYProgress } = useScroll({
    target: ref,
    offset: ["start end", "end start"]
  });
  
  const y = useTransform(scrollYProgress, [0, 1], [0, -100 * speed]);

  return (
    <motion.div ref={ref} style={{ y }} className={className}>
      {children}
    </motion.div>
  );
}
```

**使用示例**：

```tsx
// 包裹需要视差效果的元素
<ParallaxSection speed={0.3}>
  <img src="/background-element.png" alt="" />
</ParallaxSection>
```

---

## 微交互动效

### 9. 导航菜单悬停效果

**代码实现**：

```tsx
// 在导航链接中添加
<motion.a
  href="#"
  className="relative text-[#191f33] text-[14px]"
  whileHover="hover"
>
  使用场景
  <motion.span
    className="absolute bottom-0 left-0 h-[2px] bg-gradient-to-r from-[#0f67fe] to-[#2ad1f8]"
    variants={{
      hover: { width: "100%" }
    }}
    initial={{ width: 0 }}
    transition={{ duration: 0.3 }}
  />
</motion.a>
```

---

### 10. Logo滚动悬停暂停

**代码实现**：

```tsx
// 修改Logo滚动区域
<div 
  className="flex w-[200%] animate-[scrollLeft_28s_linear_infinite] hover:pause"
  onMouseEnter={(e) => e.currentTarget.style.animationPlayState = 'paused'}
  onMouseLeave={(e) => e.currentTarget.style.animationPlayState = 'running'}
>
  {/* Logo内容 */}
</div>
```

**CSS添加**：

```css
/* src/styles/index.css */
@keyframes scrollLeft {
  from { transform: translateX(0); }
  to { transform: translateX(-50%); }
}

.hover\:pause:hover {
  animation-play-state: paused !important;
}
```

---

### 11. 视频播放按钮脉冲

**代码实现**：

```typescript
// src/components/animations/PulsePlayButton.tsx
import { motion } from "framer-motion";

export function PulsePlayButton({ onClick }: { onClick?: () => void }) {
  return (
    <motion.button
      onClick={onClick}
      className="relative size-[80px] rounded-full bg-white/90 backdrop-blur-sm flex items-center justify-center"
      whileHover={{ scale: 1.1 }}
      whileTap={{ scale: 0.9 }}
    >
      {/* 脉冲圆环 */}
      <motion.div
        className="absolute inset-0 rounded-full border-4 border-white"
        animate={{
          scale: [1, 1.5, 1],
          opacity: [0.8, 0, 0.8],
        }}
        transition={{
          duration: 2,
          repeat: Infinity,
          ease: "easeInOut"
        }}
      />
      
      {/* 播放图标 */}
      <svg className="w-[32px] h-[32px] ml-1" viewBox="0 0 24 24" fill="currentColor">
        <path d="M8 5v14l11-7z" />
      </svg>
    </motion.button>
  );
}
```

---

## 性能优化建议

### 1. 使用GPU加速属性

```typescript
// ✅ 推荐：使用 transform 和 opacity
<motion.div
  animate={{ 
    x: 100,        // transform: translateX
    y: 50,         // transform: translateY
    scale: 1.2,    // transform: scale
    opacity: 0.5   // opacity
  }}
/>

// ❌ 避免：触发重排的属性
<motion.div
  animate={{ 
    width: 200,    // 触发重排
    height: 100,   // 触发重排
    top: 50        // 触发重排
  }}
/>
```

### 2. 添加 will-change 提示

```tsx
<motion.div
  style={{ willChange: "transform, opacity" }}
  animate={{ x: 100, opacity: 0.5 }}
/>
```

### 3. 支持减少动画偏好

```typescript
// src/hooks/useReducedMotion.ts
import { useEffect, useState } from "react";

export function useReducedMotion() {
  const [prefersReducedMotion, setPrefersReducedMotion] = useState(false);

  useEffect(() => {
    const mediaQuery = window.matchMedia("(prefers-reduced-motion: reduce)");
    setPrefersReducedMotion(mediaQuery.matches);

    const handleChange = () => setPrefersReducedMotion(mediaQuery.matches);
    mediaQuery.addEventListener("change", handleChange);
    return () => mediaQuery.removeEventListener("change", handleChange);
  }, []);

  return prefersReducedMotion;
}
```

**使用示例**：

```tsx
const prefersReducedMotion = useReducedMotion();

<motion.div
  animate={{ x: prefersReducedMotion ? 0 : 100 }}
  transition={{ duration: prefersReducedMotion ? 0 : 0.5 }}
/>
```

### 4. 懒加载动画组件

```typescript
// 动态导入大型动画组件
const HeavyAnimation = lazy(() => import("./HeavyAnimation"));

<Suspense fallback={<div>Loading...</div>}>
  <HeavyAnimation />
</Suspense>
```

---

## 实现步骤

### 阶段一：核心动效（1-2天）

1. ✅ 创建 `TextReveal` 组件
2. ✅ 增强 `GlowButton` 组件
3. ✅ 创建 `TabIndicator` 组件
4. ✅ 创建 `IconPop` 和 `IconPopList` 组件
5. ✅ 创建 `CountUp` 组件

### 阶段二：增强动效（1天）

6. ✅ 创建 `StarRating` 组件
7. ✅ 创建 `FlowingGradient` 组件
8. ✅ 创建 `ParallaxSection` 组件

### 阶段三：微交互（0.5天）

9. ✅ 优化导航菜单悬停
10. ✅ Logo滚动悬停暂停
11. ✅ 创建 `PulsePlayButton` 组件

### 阶段四：集成与优化（1天）

12. 在首页中应用所有动效
13. 性能测试和优化
14. 移动端适配测试
15. 无障碍测试

---

## 组件导出配置

```typescript
// src/components/animations/index.ts
export { ButtonRipple } from "./ButtonRipple";
export { GlowButton } from "./GlowButton";
export { HoverCard } from "./HoverCard";
export { Video3D } from "./Video3D";
export { TextReveal } from "./TextReveal";
export { TabIndicator } from "./TabIndicator";
export { IconPop, IconPopList } from "./IconPop";
export { CountUp } from "./CountUp";
export { StarRating } from "./StarRating";
export { FlowingGradient } from "./FlowingGradient";
export { ParallaxSection } from "./ParallaxSection";
export { PulsePlayButton } from "./PulsePlayButton";
```

---

## 首页应用示例

```tsx
// src/imports/首页第四版最新.tsx
import { 
  TextReveal, 
  GlowButton, 
  TabIndicator,
  IconPop,
  CountUp,
  StarRating,
  FlowingGradient,
  ParallaxSection
} from "../components/animations";

// Hero区
<div className="relative">
  <FlowingGradient className="opacity-60 blur-3xl" />
  
  <div className="flex gap-[12px]">
    <TextReveal text="听脑AI，提高了" />
    <CountUp end={10} suffix="倍" />
    <TextReveal text="工作/学习效率" delay={0.8} />
  </div>
  
  <GlowButton>
    <p>免费使用</p>
  </GlowButton>
</div>

// 特性图标
<IconPop delay={0}>
  <div className="bg-[rgba(16,139,254,0.2)] rounded-full size-[28px]">
    {/* 图标 */}
  </div>
</IconPop>

// 评价区
<StarRating rating={5} />
```

---

## 验收标准

### 性能指标
- [ ] 首屏动画加载 < 500ms
- [ ] 动画帧率保持 60fps
- [ ] Lighthouse 性能分数 > 90
- [ ] 移动端流畅运行

### 用户体验
- [ ] 动画自然流畅，无卡顿
- [ ] 交互反馈及时明确
- [ ] 支持减少动画偏好
- [ ] 移动端触摸交互正常

### 视觉效果
- [ ] 动效符合品牌调性
- [ ] 视觉层次清晰
- [ ] 色彩过渡自然
- [ ] 无视觉跳跃或闪烁

---

## 总结

本方案提供了**11个核心动效组件**，覆盖了从高优先级到微交互的完整动效体系。所有组件都：

✅ 使用 GPU 加速属性  
✅ 支持减少动画偏好  
✅ 提供完整的 TypeScript 类型  
✅ 遵循性能最佳实践  
✅ 易于集成和定制  

实施后，页面质感将得到显著提升，用户体验更加流畅自然。
